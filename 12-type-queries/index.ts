// interface Config { host: string; puerto: number }

// const config: Config = {
//   host: "localhost",
//   puerto: 8080,
// }

// const config = {
//   host: "localhost",
//   puerto: 8080,
// };

// type Config = typeof config;

// console.log(typeof 3)

// type Usuario = { id: number; nombre: string; email: string };

// type Clave = keyof Usuario; // "id" | "nombre" | "email"

// const clave: Clave = "dasdasd";

// type Usuario = { id: number; nombre: string; email: string};

// // type Email = Usuario["email"];
// type IdONombre = Usuario["id" | "nombre"];
